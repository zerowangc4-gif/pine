# Pine 中文说明文档

面向使用者与贡献者：怎么跑起来、界面做什么、代码怎么分层。英文架构与协议扩展指南见根目录 `README.md` 与 `docs/`；仓库内另有一份**中文代码架构详解** `Pine应用详解.md`，适合想深度读懂各层代码、理解消息往返全过程的人。

## 这是什么

Pine 是本地编码智能体桌面应用：

- 前端：Tauri + React
- 运行时：内嵌 Node sidecar（Socket.IO）
- 核心：`@pine/agent` / `@pine/ai`（**禁止改这两个包**，只通过 `packages/runtime` 胶水调用）

目标：装好就能聊；开发时也不必改上游 agent 源码。

## 快速开始

需要 Node.js >= 22.19。

```bash
cd E:\agents\pine
pnpm install
pnpm prepare:sidecar
pnpm dev:runtime    # 终端 1：sidecar，默认端口 7821
pnpm dev:ui         # 终端 2：浏览器 UI
```

打桌面安装包（需 Rust + VS Build Tools）：

```bash
pnpm build:tauri
```

产物一般在 `dist/`。

## 界面怎么用

三栏布局：

1. **左侧配置**：模型、工作目录、工具、审批、压缩、队列、资源路径等
2. **中间对话**：消息流、工具进度、审批条、输入框
3. **右侧检查器**：状态、技能/模板、历史会话、事件、Provider 报文

顶栏可切换中英文、黑白主题，以及新建会话。

### 第一次对话前

1. 确认顶栏显示「已连接」
2. 左侧填好模型（见下文 Ollama / DeepSeek）
3. 点「更改…」选工作目录（工具读写都相对这个目录）
4. 点「开始会话」或「新建会话」
5. 在中间输入框提问

运行中：

- 输入会变成 **Steer（插话）**，在下一轮注入
- **Queue follow-up** 会在本轮正常结束后再跑
- **Stop after this turn** 优雅停；**Abort now** 立刻中止

### 工作目录

- 可浏览、校验、切换；切换后会重载技能与模板
- 「最近使用」来自已落盘会话的工作区记录

### 技能与模板

Pine **不自带**任何技能；仓库里默认是空的。没写 skill 也能用：靠系统提示 + `read` / `write` / `edit` / `bash` 工具干活。

技能是「专项说明书」，不是智商补丁。有 skill 时，系统提示里只会挂上名称和简介；模型要自己去读 `SKILL.md`，或你在右侧「资源」里点运行。

放在工作区（或用户目录）下：

- `.pine/skills/<name>/SKILL.md`
- `.pine/commands/<name>.md`

最小技能示例（放在当前工作目录的 `.pine/skills/explore/SKILL.md`）：

```markdown
---
name: explore
description: Explore an unfamiliar codebase before changing anything
---

Before editing:
1. Use the read / bash tools to locate relevant files.
2. Summarize what you found in 3–5 bullets.
3. Only then propose or make a change.
```

写好后：右侧「资源」→「重新加载」，或切换一下工作目录。配置页也可改技能搜索路径。

### 审批策略

四档：从不询问 / 改动前询问 / 每次询问 / 只读（禁止改动）。还可为 bash 配正则黑名单，以及「始终允许」某工具。

## 用本地 Ollama（界面怎么填）

先保证本机 Ollama 已启动，并且已经拉过模型：

```bash
ollama serve          # 若尚未在后台运行
ollama list           # 看本地有哪些模型名
ollama pull gemma4:e4b   # 没有的话再拉
```

然后打开 UI（`pnpm dev:runtime` + `pnpm dev:ui` → http://127.0.0.1:5173/），确认顶栏「已连接」。

在左侧 **模型** 区按下面逐项填写（字段名与界面一致）：

| 界面字段 | 填什么 | 说明 |
| --- | --- | --- |
| **API 协议** | `openai-completions` | 下拉选这个；Ollama 走 OpenAI 兼容接口 |
| **模型标识** | 如 `gemma4:e4b` | 必须和 `ollama list` 里的名字一字不差（含 tag） |
| **接口地址** | `http://127.0.0.1:11434/v1` | 注意末尾是 `/v1`，不要写成 `11434` 裸端口 |
| **服务商标识** | `ollama` | 随便写个标签即可，建议固定 `ollama` |
| **API 密钥** | 留空 | 本地 Ollama 一般不需要；也可填任意占位如 `ollama` |
| **推理型模型** | 关（除非该模型真有 thinking） | `ollama show <模型>` 里带 `thinking` 再开 |
| **支持图片输入** | 按模型能力 | 带 `vision` 再开 |

填完后点左侧的 **应用**，再选工作目录 → **开始会话**，中间输入框提问。

当前模型可选哪些思考级别，可点「测试模型」按钮查看；能否自动调工具取决于模型是否具备 `tools` 能力：

```bash
ollama show gemma4:e4b
# capabilities 里应有 tools，否则只会闲聊、不会 read/write
```

命令行自测（自己起临时 sidecar，不依赖 `dev:runtime`）：

```bash
pnpm try:ollama
pnpm try:ollama gemma4:e4b
```

## DeepSeek 等云端模型（界面怎么填）

同样在左侧 **模型** 区填写：

| 界面字段 | 填什么 |
| --- | --- |
| **API 协议** | `openai-completions` |
| **模型标识** | `deepseek-chat`（或 `deepseek-reasoner`） |
| **接口地址** | `https://api.deepseek.com/v1` |
| **服务商标识** | `deepseek` 或 `custom` |
| **API 密钥** | 你的 DeepSeek Key（`sk-...`） |

其它 OpenAI 兼容端点（硅基流动、OpenRouter 自建网关等）同理：协议选 `openai-completions`，改 **接口地址** / **模型标识** / **API 密钥**，点 **应用**。

## 代码结构（怎么读）

```
packages/protocol   UI ↔ runtime 的类型与事件（无依赖）
packages/runtime    胶水层：会话、工具、工作区、持久化、Socket.IO 服务
packages/agent      Agent 循环（只读）
packages/ai         模型提供商（只读）
apps/desktop        桌面 UI
```

### runtime 要点

- `src/session.ts`：包一层 `Agent`，接上全部钩子与方法
- `src/server/`：Socket.IO 入口 + 按域拆分的 handlers
- `src/workspace.ts`：工作目录浏览 / 校验 / 切换
- `src/smoke.ts`：端到端冒烟（假模型服务，不需要 API Key）

### desktop 要点

- `src/socket/`：Socket.IO 客户端与广播→Redux 桥
- `src/store/slices/`：按职责切开的状态
- `src/store/actions/`：所有请求型副作用
- `src/theme/`：黑白双主题 + 4px 间距 + 字号阶梯
- `src/i18n/`：仅 `zh-CN` / `en-US`
- `src/components/`：按单一职责拆分的界面模块

## 自测命令

```bash
pnpm smoke                                              # runtime 冒烟（假模型）
pnpm --filter @pine/desktop exec tsc -p tsconfig.json --noEmit
pnpm try:ollama                                         # 真本地 Ollama
DEEPSEEK_API_KEY=sk-... pnpm try:deepseek               # 真 DeepSeek（无 UI）
DEEPSEEK_API_KEY=sk-... pnpm demo:ui                    # 一键：起服务 + Playwright UI 演示
DEEPSEEK_API_KEY=sk-... pnpm demo:ui:tour               # 仅跑演示（需已开 runtime/UI）
```

`demo:ui` 会自动启动 `dev:runtime` 与 `dev:ui`（若端口已被占用则复用），再跑 Playwright。密钥可放在仓库根目录 `.env`（见 `.env.example`），或临时写入环境变量。首次还需安装浏览器：

```bash
pnpm playwright:install
```

## 模型数据：只读包 `packages/ai` 的生成物

`packages/ai` 是只读核心，手写源码别改。但它有一块内容是**自动生成且不进 git** 的产物：
各模型提供商的目录数据在 `packages/ai/src/providers/data/*.json`，供 `*.models.ts`
（以及顶层 `models.generated.ts` / `image-models.generated.ts`）构建期引用。因为 `data/`
被 `.gitignore` 忽略，全新 clone 后它不存在——此时单独对 `packages/ai` 跑 `tsc` 会报成片的
`unknown` / `ModelGroups` 约束错误。**这不是代码坏了，是数据没 hydrate。**

仓库根已封装对应命令（只写回那个被忽略的 `data/` 目录，不碰核心源码）：

```bash
pnpm check:model-data       # 校验现有 data/ 与源码结构一致（快速、无网络）
pnpm hydrate:model-data     # 拉取补全 data/*.json——新 clone 后必跑一次
pnpm generate:models        # 完整重新生成：data/ + models.generated.ts 汇总（联网、较慢）
pnpm generate:image-models  # 单独刷新图像模型子集
```

日常建议：clone 完先 `pnpm hydrate:model-data` 再 `pnpm typecheck`；只有需要跟上最新
提供商目录时才跑更重的 `generate:models`。

## 设计约束（贡献时请遵守）

1. **不改** `packages/agent`、`packages/ai`
2. 新能力走 protocol 事件 → runtime handler → desktop action/UI
3. 通信只用 Socket.IO，不要再引入裸 WebSocket
4. UI 样式只用 styled-components；间距走 `theme.space`
5. 文案必须同时有中英键；以 `en-US.ts` 为键集合权威来源
6. 行为变更尽量补 `smoke.ts` 场景

## 常见问题

**顶栏一直「连接中」**  
先起 `pnpm dev:runtime`。端口默认 `7821`，可用环境变量 `PINE_RUNTIME_PORT` 改。

**提示协议版本不匹配**  
UI 与 sidecar 不是同一套构建；重启两边或重新 `prepare:sidecar`。

**Ollama 只会闲聊、不调工具**  
先确认能力与配置，再看请求里有没有带上 tools：

1. `ollama show <你的模型名>` 的 Capabilities 里必须有 `tools`（若是 `gemma4:e4b` 这类小模型，位置在 capabilities 段）。
2. 左侧 **工具** 区：至少打开 `read`（建议 `read`/`write`/`edit`/`bash` 都开），然后点 **应用**。
3. 提问要写死，小模型很挑措辞，例如：  
   `请先用 read 工具读取 notes.txt，再回答里面写了什么。不要猜。`
4. 看右侧 **事件**：应出现 `tool_execution_start` / `tool_execution_end`。  
   若只有 `message_*` 没有 tool，说明模型这轮没发起调用。
5. 打开配置里的 **调试报文**（debug payloads）再问一次，右侧 Provider 请求里应有 `tools` 数组；若没有，说明运行时没把工具注册进去（多半是没点应用，或会话是旧配置打开的——新建会话再试）。
6. **接口地址** 必须是 `http://127.0.0.1:11434/v1`（带 `/v1`）。

命令行可自证链路（不依赖你当前 UI 会话）：

```bash
pnpm try:ollama gemma4:e4b
```

若这里 PASS 且带 `(used tools)`，而 UI 里不调，问题在界面配置/会话，不在模型。

小模型还常有「上一轮读过就不肯再调工具」——这是能力问题，不是 skill。编码任务优先 DeepSeek 更稳。

另一种更糟的表现：模型在回复里**假装**调用工具（写成 \`\`\`bash read xxx\`\`\` 或「请把代码贴给我」），右侧事件却没有 `tool_execution_*`。这是在演工具，不是真调用。遇到这种：**新建会话**、确认工作目录指到目标项目、系统提示已强调必须真调工具；仍不行就换 DeepSeek。

**本地模型感觉很笨**  
多半不是 skill 没写。Pine 默认就没有技能；没 skill 也能编程。更常见原因：

1. **模型本身偏小**：如 `gemma4:e4b` 大约 8B，工具调用、多步推理明显弱于 DeepSeek 云端。
2. **没真正调工具**：右侧「事件」里若看不到 `tool_execution_*`，它在猜而不是读文件。
3. **工作目录不对**：工具都相对工作区；选错目录会读空/读错。
4. **配置没点「应用」**。

想稳一点：编码任务先用 DeepSeek；本地模型适合试工具链路或隐私场景。要专项流程再按上文加 `.pine/skills/.../SKILL.md`。

**改了配置好像没生效**  
多数字段要点「应用」。思考级别与工作目录是例外，会立刻下发。
